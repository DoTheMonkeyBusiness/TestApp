CREATE TABLE DbImageData (
  id INTEGER NOT NULL PRIMARY KEY AUTOINCREMENT,
  url TEXT NOT NULL
);

get:
SELECT * FROM DbImageData WHERE id = ?;

getAll:
SELECT * FROM DbImageData;

insert:
INSERT INTO DbImageData(url) VALUES (?);

deleteAll:
DELETE FROM DbImageData;